(() => {
  const apiBase=(window.APP_CONFIG?.API_BASE_URL||"").replace(/\/$/,"");
  const readSession=()=>{for(const s of[localStorage,sessionStorage]){try{const raw=s.getItem("lacvietgamesStoreSession");if(raw)return JSON.parse(raw)}catch{}}return null};
  const esc=(v="")=>String(v).replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
  const fmt=n=>Number(n||0).toLocaleString("vi-VN");
  const money=n=>`${Number(n||0).toLocaleString("vi-VN")}đ`;

  async function api(path,{method="GET",body,auth=true}={}){
    const session=readSession();const headers={"Content-Type":"application/json"};if(auth&&session?.token)headers.Authorization=`Bearer ${session.token}`;
    const response=await fetch(`${apiBase}${path}`,{method,headers,body:body?JSON.stringify(body):undefined});
    const payload=await response.json().catch(()=>null);
    if(!response.ok||payload?.success===false)throw Object.assign(new Error(payload?.message||"Yêu cầu không thành công."),{status:response.status,code:payload?.code});
    return payload;
  }

  const style=document.createElement("style");style.textContent=`
    .auth-link-button{border:0;background:none;color:#82aaff;font:inherit;padding:0;cursor:pointer;font-weight:700;text-align:left}.auth-link-button:hover{text-decoration:underline}
    .wallet-professional{display:grid;gap:28px}.wallet-summary{padding:32px;border-radius:24px;background:linear-gradient(135deg,#5139c8,#286bd2);box-shadow:0 20px 55px rgba(24,49,127,.28)}.wallet-summary small{opacity:.8}.wallet-summary strong{display:block;font-size:clamp(34px,5vw,58px);margin-top:8px}.wallet-pack-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:14px}.wallet-pack-card{border:1px solid #273248;background:#111723;color:#fff;border-radius:18px;padding:22px;text-align:left;cursor:pointer;transition:.2s}.wallet-pack-card:hover{transform:translateY(-2px);border-color:#577de8;background:#151e31}.wallet-pack-card .coin{font-size:28px}.wallet-pack-card strong{display:block;font-size:22px;margin:14px 0 6px}.wallet-pack-card small{color:#9fb2df}.wallet-section{background:#101620;border:1px solid #252d3d;border-radius:22px;padding:24px}.wallet-section-head{display:flex;align-items:center;justify-content:space-between;gap:16px;margin-bottom:16px}.wallet-section-head h2{margin:0}.transaction{display:grid;grid-template-columns:auto 1fr auto;gap:14px;align-items:center;padding:15px 0;border-bottom:1px solid #202a3b}.transaction:last-child{border-bottom:0}.transaction small{display:block;color:#8898b3;margin-top:4px}.transaction .positive{color:#70dea0}.transaction .negative{color:#ff8e9d}.wallet-empty{padding:24px 0;color:#8391a9;text-align:center}
    .profile-settings-grid{display:grid;grid-template-columns:minmax(0,1.2fr) minmax(280px,.8fr);gap:22px}.settings-card{background:#101620;border:1px solid #252d3d;border-radius:22px;padding:24px}.settings-card h2,.settings-card h3{margin-top:0}.settings-field{display:grid;gap:8px;margin:16px 0}.settings-field label{font-weight:700}.settings-field input{width:100%;box-sizing:border-box;border:1px solid #344158;background:#0c1320;color:#fff;padding:13px 14px;border-radius:12px;font:inherit}.settings-field input[readonly]{opacity:.7}.settings-help{color:#8795ad;font-size:13px;line-height:1.5}.settings-row{display:flex;gap:10px;align-items:center;flex-wrap:wrap}.settings-status{min-height:22px;margin-top:12px;color:#8edba6}.settings-status.error{color:#ff929f}.activity-item{padding:13px 0;border-bottom:1px solid #202a3b}.activity-item:last-child{border-bottom:0}.activity-item b{display:block}.activity-item small{color:#8492aa}.email-verify-box{padding:16px;border:1px solid #355c9f;background:#101d35;border-radius:14px;margin-top:16px}
    @media(max-width:850px){.profile-settings-grid{grid-template-columns:1fr}}
  `;document.head.appendChild(style);

  function enhanceAuthModal(modal){
    if(!modal||modal.dataset.passwordEnhanced)return;modal.dataset.passwordEnhanced="1";
    const login=modal.querySelector("#serverLoginForm");const card=modal.querySelector(".server-auth-card");const status=modal.querySelector("#serverAuthStatus");if(!login||!card)return;
    const submit=login.querySelector('button[type="submit"]');
    const forgot=document.createElement("button");forgot.type="button";forgot.className="auth-link-button";forgot.textContent="Quên mật khẩu?";submit.before(forgot);
    const forgotForm=document.createElement("form");forgotForm.id="serverForgotForm";forgotForm.className="server-auth-form";forgotForm.hidden=true;forgotForm.innerHTML=`<h2>Khôi phục mật khẩu</h2><p>Nhập email tài khoản để nhận mã xác thực.</p><label>Email<input id="serverForgotEmail" type="email" required></label><button class="btn btn-primary" type="submit">Gửi mã xác thực</button><button class="btn btn-secondary" type="button" data-password-back>Quay lại đăng nhập</button>`;
    const resetForm=document.createElement("form");resetForm.id="serverResetForm";resetForm.className="server-auth-form";resetForm.hidden=true;resetForm.innerHTML=`<h2>Đặt lại mật khẩu</h2><p>Mã xác thực có hiệu lực trong 15 phút.</p><label>Email<input id="serverResetEmail" type="email" readonly></label><label>Mã 6 chữ số<input id="serverResetCode" inputmode="numeric" maxlength="6" required></label><label>Mật khẩu mới<input id="serverResetPassword" type="password" minlength="8" required></label><label>Xác nhận mật khẩu<input id="serverResetConfirm" type="password" minlength="8" required></label><button class="btn btn-primary" type="submit">Cập nhật mật khẩu</button><button class="btn btn-secondary" type="button" data-password-back>Quay lại đăng nhập</button>`;
    status.before(forgotForm,resetForm);
    const showLogin=()=>{forgotForm.hidden=true;resetForm.hidden=true;const main=modal.querySelector("#serverAuthMain");main.hidden=false;modal.querySelector("#serverLoginForm").hidden=false;modal.querySelector("#serverRegisterForm").hidden=true;modal.querySelectorAll("[data-auth-tab]").forEach(b=>b.classList.toggle("active",b.dataset.authTab==="login"));};
    forgot.addEventListener("click",()=>{modal.querySelector("#serverAuthMain").hidden=true;forgotForm.hidden=false;resetForm.hidden=true;forgotForm.querySelector("#serverForgotEmail").value=modal.querySelector("#serverLoginEmail")?.value||"";status.textContent=""});
    modal.addEventListener("click",e=>{if(e.target.closest("[data-password-back]"))showLogin()});
    forgotForm.addEventListener("submit",async e=>{e.preventDefault();const email=forgotForm.querySelector("#serverForgotEmail").value.trim().toLowerCase();const btn=forgotForm.querySelector('button[type="submit"]');btn.disabled=true;btn.textContent="Đang gửi...";try{const r=await api("/api/Accounts/forgot-password",{method:"POST",body:{email},auth:false});forgotForm.hidden=true;resetForm.hidden=false;resetForm.querySelector("#serverResetEmail").value=email;status.textContent=r.message||"Mã xác thực đã được gửi.";status.classList.add("success")}catch(err){status.textContent=err.message;status.classList.remove("success")}finally{btn.disabled=false;btn.textContent="Gửi mã xác thực"}});
    resetForm.addEventListener("submit",async e=>{e.preventDefault();const email=resetForm.querySelector("#serverResetEmail").value;const code=resetForm.querySelector("#serverResetCode").value.trim();const password=resetForm.querySelector("#serverResetPassword").value;const confirm=resetForm.querySelector("#serverResetConfirm").value;if(!/^\d{6}$/.test(code)){status.textContent="Mã xác thực phải gồm 6 chữ số.";return}if(password.length<8){status.textContent="Mật khẩu phải có ít nhất 8 ký tự.";return}if(password!==confirm){status.textContent="Mật khẩu xác nhận không khớp.";return}const btn=resetForm.querySelector('button[type="submit"]');btn.disabled=true;btn.textContent="Đang cập nhật...";try{const r=await api("/api/Accounts/reset-password",{method:"POST",body:{email,code,newPassword:password},auth:false});showLogin();modal.querySelector("#serverLoginEmail").value=email;status.textContent=r.message||"Mật khẩu đã được cập nhật.";status.classList.add("success")}catch(err){status.textContent=err.message;status.classList.remove("success")}finally{btn.disabled=false;btn.textContent="Cập nhật mật khẩu"}});
  }

  const observer=new MutationObserver(()=>enhanceAuthModal(document.getElementById("serverAuthModal")));observer.observe(document.documentElement,{childList:true,subtree:true});

  async function renderWallet(){
    if(document.body.dataset.page!=="wallet")return;const app=document.getElementById("app");const session=readSession();if(!app||!session?.token)return;
    try{
      const [wallet,packs]=await Promise.all([api("/api/store/wallet/transactions"),api("/api/store/payments/packs",{auth:false})]);
      app.innerHTML=`<div class="wallet-professional"><div><span class="eyebrow">LacViet Wallet</span><h1>Ví Lạc Coin</h1></div><section class="wallet-summary"><small>Số dư khả dụng</small><strong>${fmt(wallet.balance)} LC</strong></section><section><div class="wallet-section-head"><div><h2>Nạp Lạc Coin</h2><p>Chọn gói phù hợp và thanh toán an toàn.</p></div></div><div class="wallet-pack-grid">${(packs.data||[]).map(p=>`<button class="wallet-pack-card" data-pack="${p.coinAmount}" type="button"><span class="coin">🪙</span><strong>${fmt(p.coinAmount)} LC</strong><small>${money(p.amountVnd)}</small></button>`).join("")}</div></section><section class="wallet-section"><div class="wallet-section-head"><h2>Lịch sử giao dịch</h2></div><div class="transaction-list">${wallet.data?.length?wallet.data.map(t=>`<div class="transaction"><span>🪙</span><div><b>${esc(t.description||t.type)}</b><small>${new Date(t.createdAt).toLocaleString("vi-VN")}${t.moneyAmount?` · ${money(t.moneyAmount)}`:""}</small></div><b class="${t.coinAmount>=0?"positive":"negative"}">${t.coinAmount>=0?"+":""}${fmt(t.coinAmount)} LC</b></div>`).join(""):`<div class="wallet-empty">Chưa có giao dịch.</div>`}</div></section></div>`;
    }catch(err){app.innerHTML=`<div class="empty-state"><h2>Không thể mở ví</h2><p>${esc(err.status===401?"Phiên đăng nhập đã hết hạn. Vui lòng đăng nhập lại.":err.message)}</p><button class="btn btn-primary" data-open-server-auth>Đăng nhập</button></div>`}
  }

  async function renderProfileSettings(){
    const page=document.body.dataset.page;if(page!=="profile"&&page!=="editProfile")return;const session=readSession();if(!session?.token)return;
    try{
      const profile=(await api("/api/store/profile")).data;
      document.querySelectorAll(".account-btn span:last-child").forEach(el=>el.textContent=profile.effectiveDisplayName);
      if(page==="profile"){
        const card=document.querySelector(".profile-card");if(card){const h2=card.querySelector("h2"),p=card.querySelector("p");if(h2)h2.textContent=profile.effectiveDisplayName;if(p)p.textContent=profile.email;}
        return;
      }
      const app=document.getElementById("app");if(!app)return;const activities=(await api("/api/store/profile/activities")).data||[];
      app.innerHTML=`<div class="section-head"><div><span class="eyebrow">Account Settings</span><h1>Chỉnh sửa hồ sơ</h1></div><a class="btn btn-secondary" href="./profile.html">Quay lại hồ sơ</a></div><div class="profile-settings-grid"><section class="settings-card"><h2>Thông tin cá nhân</h2><div class="settings-field"><label>Tên tài khoản</label><input value="${esc(profile.name)}" readonly><span class="settings-help">Tên tài khoản gốc.</span></div><form id="displayNameForm"><div class="settings-field"><label>Tên hiển thị</label><input id="displayNameInput" maxlength="100" value="${esc(profile.displayName||"")}" placeholder="${esc(profile.name)}"><span class="settings-help">Để trống sẽ tự động sử dụng tên tài khoản.</span></div><button class="btn btn-primary" type="submit">Lưu tên hiển thị</button></form><div id="profileStatus" class="settings-status"></div><hr style="border:0;border-top:1px solid #252d3d;margin:26px 0"><h2>Email đăng nhập</h2><div class="settings-field"><label>Email hiện tại</label><input value="${esc(profile.email)}" readonly></div><form id="emailChangeForm"><div class="settings-field"><label>Email mới</label><input id="newEmailInput" type="email" placeholder="emailmoi@example.com" required></div><button class="btn btn-secondary" type="submit">Gửi mã xác thực</button></form><div id="emailVerifyArea" ${profile.pendingEmail?"":"hidden"} class="email-verify-box"><b>Xác thực email mới</b><p id="pendingEmailText">${profile.pendingEmail?`Mã đã được gửi tới ${esc(profile.pendingEmail)}.`:""}</p><form id="confirmEmailForm" class="settings-row"><input id="emailCodeInput" class="field-control" inputmode="numeric" maxlength="6" placeholder="Mã 6 chữ số" required><button class="btn btn-primary" type="submit">Xác nhận email</button></form></div></section><aside class="settings-card"><h3>Hoạt động tài khoản</h3><div id="activityList">${activities.length?activities.map(a=>`<div class="activity-item"><b>${esc(a.description)}</b><small>${new Date(a.createdAt).toLocaleString("vi-VN")}</small></div>`).join(""):`<div class="wallet-empty">Chưa có hoạt động.</div>`}</div></aside></div>`;
      const status=document.getElementById("profileStatus");const setStatus=(m,e=false)=>{status.textContent=m;status.classList.toggle("error",e)};
      document.getElementById("displayNameForm").addEventListener("submit",async e=>{e.preventDefault();try{const r=await api("/api/store/profile/display-name",{method:"PUT",body:{displayName:document.getElementById("displayNameInput").value}});setStatus(r.message);session.displayName=r.data.displayName;session.effectiveDisplayName=r.data.effectiveDisplayName}catch(err){setStatus(err.message,true)}});
      document.getElementById("emailChangeForm").addEventListener("submit",async e=>{e.preventDefault();try{const r=await api("/api/store/profile/email-change/request",{method:"POST",body:{newEmail:document.getElementById("newEmailInput").value.trim().toLowerCase()}});document.getElementById("emailVerifyArea").hidden=false;document.getElementById("pendingEmailText").textContent=`Mã đã được gửi tới ${r.data.pendingEmail}.`;setStatus(r.message)}catch(err){setStatus(err.message,true)}});
      document.getElementById("confirmEmailForm").addEventListener("submit",async e=>{e.preventDefault();try{const r=await api("/api/store/profile/email-change/confirm",{method:"POST",body:{code:document.getElementById("emailCodeInput").value.trim()}});for(const storage of[localStorage,sessionStorage]){try{const raw=storage.getItem("lacvietgamesStoreSession");if(raw){const s=JSON.parse(raw);s.email=r.data.email;storage.setItem("lacvietgamesStoreSession",JSON.stringify(s))}}catch{}}setStatus(r.message);setTimeout(()=>location.reload(),700)}catch(err){setStatus(err.message,true)}});
    }catch{}
  }

  const boot=()=>{setTimeout(renderWallet,60);setTimeout(renderProfileSettings,60);enhanceAuthModal(document.getElementById("serverAuthModal"))};
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",boot,{once:true});else boot();
})();
